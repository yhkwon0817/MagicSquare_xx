# MagicSquare_xx — STEP 1 Mom Test + 세션 3 워크북 보고서

**작성 목적**: Mom Test 인터뷰(`Report/01`) 결과를 바탕으로 **세션 3 워크북**(R-G-I-O · 성공 기준 · 8계층 범위)을 확정한다.  
**범위**: STEP 1 → 세션 3 — Rule · Command · (Skill) · Test Loop (`validate_lines`)  
**산출물 성격**: `docs/PRD.md` 초안 및 `validate_lines` 구현의 입력 근거.

---

## 문서 메타데이터

| 항목 | 내용 |
|------|------|
| 문서 ID | `03.MagicSquare_Session3_Workbook_Report` |
| 프로젝트 ID | MagicSquare_xx |
| 단계 | STEP 1 — Mom Test → 세션 3 워크북(R-G-I-O) |
| 작성일 | 2026-06-10 |
| 선행 문서 | [`01.MagicSquare_MomTest_Report.md`](./01.MagicSquare_MomTest_Report.md) |
| 대화 Export | [`Prompting/03.MagicSquare_Session3_Workbook-Transcript.md`](../Prompting/03.MagicSquare_Session3_Workbook-Transcript.md) |

---

## 1. 요약

| 구분 | 결과 |
|------|------|
| Mom Test 인터뷰 | Q1~Q10 완료 (과거 사실 F-1~F-8) |
| Good Data | **대각선 누락 + ~20분 비용 + 표 재작업 + 2차 전 항목 확인 후 제출·정답** |
| 워크북 채점 | **8.0 / 10** |
| **판정** | **세션 3 범위 확정** — R-G-I-O · 성공 기준 3개 · Rule/Command/Test Loop |

Mom Test **진짜 문제**를 **판정·확인 비용** 언어로 번역해, 세션 3에서 `validate_lines` + Test Loop만 구현한다.

---

## 2. Mom Test 결과 (입력)

### 2.1 페르소나

| 항목 | 내용 |
|------|------|
| **한 줄 정의** | 4×4 **부분 마방진**(빈칸 2개, 1~16, 합 34)을 과제·연습에서 **손 계산** 또는 코드로 다루는 **학습자** |
| **행동 패턴** | 1차 시도 검산 누락(대각) → **~20분 손실** → **표 전면 재작업** → **행·열·대각·중복·범위 전부 수기 재확인** → 제출·정답 |
| **성공 신호** | 행·열·**두 대각**·중복·범위까지 확인한 뒤 제출·정답 |

### 2.2 진짜 문제 (한 문장)

> **부분 마방진을 처음 맞출 때, 행·열·대각을 빠짐없이 검증했다는 확신이 없어 시간을 쓰고 표를 다시 그린 뒤에야, 손으로 전 항목을 다시 더해 “맞다”고 끝낼 수 있다.**

### 2.3 Mom Test 증거 3줄

| # | 증거 | 근거 ID |
|---|------|---------|
| 1 | **대각선 하나를 빼먹어 약 20분**을 날렸다 | F-2 |
| 2 | **혼자 다시 더해** 알고 **표를 처음부터 다시** 그렸다 | F-4, F-5 |
| 3 | 두 번째엔 **행·열·대각·중복·범위까지** 확인한 뒤 **제출·정답**으로 끝났다 | F-6, F-7 |

---

## 3. 세션 3 워크북 (Mom Test → R-G-I-O)

### 3.1 주제 (1문장)

> **4×4 부분 마방진에서 빈칸을 채운 직후, 행·열·대각선 10선이 각각 34인지 빠르게 확정하고, 틀리면 어느 줄인지 바로 짚을 수 있게 검증 규칙과 테스트 루프를 만든다.** 미완성 격자(0 포함)는 `incomplete`로 구분한다.

*(솔루션 최소화 — 앱·솔버·UI가 아니라 **판정·확인 비용**만 다룸)*

### 3.2 R-G-I-O

| | 내용 |
|---|---|
| **R — Role** | 4×4 **부분** 마방진 학습자. 빈칸 2개(0)를 1~16으로 채운 뒤 **맞았는지 스스로 확인**해야 함 |
| **G — Goal** | 빈칸 채운 후 **10선 합 34 여부를 즉시 판정**하고, 틀리면 **어느 행·열·대각선**인지 식별 *(20분 낭비 → 처음·한 번에)* |
| **I — Input** | `grid: list[list[int]]` — 4×4 정수 격자. 셀 값 **0**(빈칸) 또는 **1~16** |
| **O — Output** | `validate_lines(grid)` → **ValidationResult**: `ok` (bool), `status` (`"pass"` \| `"fail"` \| `"incomplete"`), `lines[]` — fail 시 **틀린 줄만** `{ id, sum, expected: 34 }`. 줄 ID: `row:0`~`row:3`, `col:0`~`col:3`, `diag:main`, `diag:anti`. **R5:** 격자에 `0`이 하나라도 있으면 `status=incomplete`, 합 계산·34 비교는 수행하지 않음 |

### 3.3 성공 기준 3개 (Mom Test 증거 연결)

| # | 성공 기준 | 연결 증거 |
|---|-----------|-----------|
| 1 | **`validate_lines(grid)`** 한 번으로 **10선×34 검증**을 수행한다 (행 4 + 열 4 + 대각 2) | ① “행·열·**대각선** 합 맞췄는데 **대각선 하나를 빼먹어서**” |
| 2 | Test Loop **Red→Green**: Red — 합≠34 → `ok=false`, `status=fail`; Red — `0` 포함 → `status=incomplete`; Green — 정답 격자 → `ok=true`, `status=pass` | ① “**34가 안 맞아 20분**” → 같은 실패를 테스트로 **1번에 재현** |
| 3 | fail 시 `lines[]`에 **틀린 줄 ID**(`row:2`, `diag:main` 등)와 `sum`, `expected: 34`를 반환한다 (10선 전부 검사, 줄 누락 없음) | ② “맞는지 **판정하지 못하고**” 같은 시도 반복 → **줄 단위**로 짚어야 함. ③ 2차 **전 항목 확인** 후 제출·정답 → 1차에도 **10선 판정 확신** 필요 |

### 3.4 표면 문제 — 이번 프로젝트에서 하지 않을 것

| 하지 않음 | 이유 (Mom Test) |
|-----------|-----------------|
| BCE 전체 / `GridUI` / `InputHandler` | UI·입력은 **표면 솔루션** — 이번 고통은 **판정·확인** |
| `Solver` / 빈칸 자동 채우기 / 힌트 | 학습자 고통은 **풀기**보다 **맞는지 확인** |
| BCE 분류·클래스 설계만 하고 테스트 없음 | 과제는 **설계표** — 검증·재현 없음 |
| 1~16 **중복·범위** 검증까지 한꺼번에 | 세션 3 범위 밖 — **합 34 판정**에 집중 *(증거 ③의 중복·범위는 세션 4+)* |

### 3.5 8계층 — 이번 세션에서 만들 것만

| 계층 | 세션 3에서 할 일 | Mom Test 연결 |
|------|------------------|---------------|
| **Rule** | R1~R5: **행·열·대각선 합 = 34** 판정. R5 — 0(빈칸) 포함 시 `status=incomplete` (**fail과 구분**, 합 검증 생략) | “34가 안 맞았는지” **판정 기준**을 코드·문서로 고정 |
| **Command** | `validate_lines(grid)` → `ValidationResult` — 10선 합 계산 + 34 비교 + fail 시 **틀린 줄 ID·sum·expected** 반환 | 합 확인에 **시간 많이** → **한 Command**로 대체 |
| **(Skill)** | *(선택)* pytest 실행 / 격자 fixture 준비 Skill | 매번 손으로 10선 더하기 제거 |
| **Test Loop** | Red: 합≠34 → `ok=false`, `status=fail`, **실패 줄** assert; Red: `0` 포함 → `status=incomplete`; Green: 정답 격자 → `ok=true`, `status=pass` | ① **20분 낭비** → **같은 실패를 1번에 재현** |

**이번 세션에서 만들지 않음:** Entity(`MagicSquare`/`Cell`), Boundary(`ResultDisplay`), Solver, MissingFinder, 전체 BCE

---

## 4. 워크북 채점

| 체크 | 판정 | 근거 |
|------|------|------|
| Mom Test → 주제 연결 | ✅ | 판정·확인 비용 중심, 솔루션 최소화 |
| R-G-I-O 입출력 명확 | ✅ | `grid` → `ValidationResult` + 줄 ID |
| 성공 기준 ↔ 증거 3줄 | ✅ | 10선·Test Loop·줄 단위 Output |
| 표면 문제 분리 | ✅ | Solver/UI/BCE/중복·범위 제외 |
| 8계층 범위 | ✅ | Rule · Command · (Skill) · Test Loop만 |
| F-3 보완 (20분 내 행동) | △ | 미수집 — 선택적 추가 인터뷰 |

**점수: 8.0 / 10**

---

## 5. 결론 및 다음 단계

### 결론

- Mom Test **Good Data** 확보: 대각 누락 → 20분 → 표 재작업 → 2차 전 항목 확인 → 제출·정답.
- **진짜 문제**를 세션 3 **10선 합 34 판정**으로 번역.
- **세션 3 워크북** 확정: 주제 · R-G-I-O · 성공 기준 3개 · Rule/Command/Test Loop 범위.

### 권장 액션

1. **Rule + Command + Test Loop 구현** — `validate_lines` 우선
2. **`docs/PRD.md` 초안** — 본 워크북 §3을 PRD §1~§4로 이식
3. **Solver / UI / BCE 전체** — 세션 3 이후
4. **(선택)** F-3 보완 인터뷰 — 20분 동안의 구체 행동 1회

---

## 6. 관련 문서

| 문서 | 설명 |
|------|------|
| [01.MagicSquare_MomTest_Report.md](./01.MagicSquare_MomTest_Report.md) | STEP 1 Mom Test 인터뷰 보고서 |
| [Prompting/01.MagicSquare_MomTest_Interview-Transcript.md](../Prompting/01.MagicSquare_MomTest_Interview-Transcript.md) | Mom Test Q1~Q10 대화 Export |
| [Prompting/03.MagicSquare_Session3_Workbook-Transcript.md](../Prompting/03.MagicSquare_Session3_Workbook-Transcript.md) | 세션 3 워크북 대화 Export |

---

*본 문서는 `Report/03.MagicSquare_Session3_Workbook_Report.md` — MagicSquare_xx STEP 1 Mom Test + 세션 3 워크북 보고서입니다.*
